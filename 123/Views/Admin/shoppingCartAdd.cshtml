@model ShoppingCartViewModel
<div class="modal fade bd-example-modal-lg" id="cart-add" tabindex="-1" role="dialog" aria-labelledby="exampleModalLongTitle" aria-hidden="true">
                                <div class="modal-dialog modal-lg" role="document">
                                    <div class="modal-content">
                                    <div class="modal-header">
                                        <h5 class="modal-title" id="exampleModalLongTitle">Create Users</h5>
                                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                        <span aria-hidden="true">&times;</span>
                                        </button>
                                    </div>
                                    <div class="modal-body">
                                        <form asp-action="add" method="post" id="user--add">
                    <input type="hidden" name='cart_id' asp-for="ShoppingCart.CartId">
                                            <div class="row">
                                                <div class="col">
                                                     <div class="form-group">
                                                        <label >user_id</label>
                                                        <select class="form-control" name="UserId" asp-for="ShoppingCart.UserId" asp-items="@(new SelectList(ViewBag.Users ?? new List<User>(), "UserId", "UserName"))">
                                                        </select>
                                                    </div>
                                                </div>
                                            </div>
                                        </form>
                                    </div>
                                    <div class="modal-footer">
                                        <button type="button" class="btn btn-outline-secondary" data-dismiss="modal" style="border-radius: 20px;">Close</button>
                                        <button type="submit" class="btn btn-outline-primary" style="border-radius: 20px;" type="submit" form="user--add">Save changes</button>
                                    </div>
                                    </div>
                                </div>
                            </div>